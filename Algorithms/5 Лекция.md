## Реализация выборки элементов из массива в список
Дан массив целых чисел. Построить список из элементов массива, принадлежащих диапазону \[x,y\]
```c++
#include <iostream>
struct list {
 int info;
 list* next;
 list* prev;
};

list* beg = new list;
void insert(int* a, int n) 
{
  for (int i = 0; i < n; ++i) {
    cin >> a[i];
  }
}
void reform(list *p, int* a, int n) {
  p = beg;
  list* r;
  for (int i = 0;i < n; ++i) {
    if (a[i] >= x && a[i] <= y) {
      r = new list;
      r->info = a[i];
      p->next = r; r->prev = p;
      r->next = NULL; p = r;
    }
  }
}
```

```c++
void output(list* p) {
  while (p != nullptr) {
    cout << p->info;
    p = p->next
  }
}

void deleteall() {
if (beg != nullptr) {
  beg = beg->next;
  deleteall();
  delete beg;
}
```

```c++
int main() {
  list* p = new list;
  list* p2 = new list;
  insert(p);
  p = beg;
  reform(p2,p);
  p2 = beg;
  output(p2);
  deleteall();
}
```

---
Реализация выборки элементов из кольцевого списка в линейный список (кольцевой двунаправленный список дан)

```c++
struct list {
  int info;
  list* next = nullptr; list* prev = nullptr;
};

list* beg = new list;
list* beg2 = new list;

void insert(list* p) {
  int x; cin >> x;
  if (x != 0) {
    beg->info = x;
    beg->next = beg;
    beg->prev = beg;
    cin >> x;
    p = beg;
    while (x) {
      list* r = new list;
      r->info = x;
      r->next = beg;
      r->prev = p;
      p->next = r;
      p = r;
      cin >> x;
    }
    beg->prev = p;
    p->next = beg;
  }
}
```

```c++
void reform(list* p2, list* p) {
  p2 = beg2; list* r2;
  p = beg;
  do {
    if (p->prev != nullptr && условие) {
      r2 = new list;
      r2->info = p->info;
      r2->next = nullptr;
      r2->prev = p2;
      p2->next = r2;
      p2 = r2;
    }
    p = p->next;
  } while (p != beg);
  if (beg2->next != nullptr) {
    r2 = beg2;
    beg2 = beg2->next;
    beg2->prev = nullptr;
    delete r2;
  }
  else {
    beg2 = nullptr;
  }
}
```

---
## Реализация стека с помощью линейного списка

```c++
struct Stack {
  int info;
  Stack *next;
};

// Создание стека
Stack *CreateStack() {
  return nullptr;
}

// Проверка на пустоту
bool IsEmptyStack(Stack* top) {
  return top == nullptr;
}

// Включение нового элемента в стек
void StackPush(int data, Stack* &top) {
  Stack *r = new Stack;
  r->info = data;
  r->next = top;
  top = r;
}

// Извлечение элемента из стека
int StackPop(Stack* &top) {
  if (!IsEmptyStack(top)) {
    Stacck* r = top;
    int x = r->info;
    top = top->next;
    delete r;
    return x;
  }
  return NAN;
  // Можно любое "своё" значение
}

//Печать и удаление стека
void printStack(Stack* &top) {
  while (!IsEmptyStack(top)) {
    cout << StackPop(top);
  }
}

void DeleteStack(Stack* &top) {
  while (!IsEmptyStack(top)) StackPop;
}
```

### Реализация стека с помощью линейного списка
...
## Реализация очереди с помощью линейного списка
```c++
#include <iostream>
using namespace std;
struct Queue {
  int info;
  Queue* next;
};

Queue* beg;
Queue* tail = new Queue;
bool isEmpty(Queue* beg) {
  return beg == nullptr;
}

int QueuePop(Queue* &beg, Queue* &tail) {
  if (!isEmpty(beg)) {
    Queue* r = beg;
    int x = beg->info;
    if (beg == tail) tail = nullptr;
    beg = beg->next;
    delete r;
    return x;
  }
  return NAN;
}
```

*Убить всех людей, которые проспали* © - Н.Ю. Добровольская 5 марта 2026 года 8:54

```c++
void QueuePush(Queue* &beg, Queue* &tail, int data){
  Queue* r = new Queue;
  if (isEmpty(beg)) {
    beg = new Queue;
    beg->info = data;
    beg->next = nullptr;
    tail = beg;
  } else {
    Queue* p = beg;
    r->info = data;
    r->next = nullptr;
    tail->next = r;
    tail = r;
  }
}
```

```c++
int main() {
  beg = nullptr; tail = nullptr;
  for (int i = 0; i < 30; ++i)
    QueuePush(beg, tail, i);
  cout << QueuePop(beg, tail);
  Print(beg, tail);
  return 0;
}
```
## Деревья
```c++
struct tree {
	int info; // информационное поле
	tree* L;
	tree* R;
};
```
Бинарное дерево - это динамическая структура данных, представляющая собой дерево, в котором каждая вершина имеет не более двух потомков.

По степени вершин бинарные деревья делятся на:
1) строгие (0 или 2 вершины)
2) нестрогие (0, 1, 2 вершины)
3) полные деревья (заполнены все уровни)
4) неполные

Деревом двоичного поиска называется корневое двоичное дерево, у которого все информационные составляющие левого поддерева **меньше** информационного значения корня, а информационные составляющие правого поддерева **больше** корня

### Функция добавления элемента в дерево
```c++
void add(tree* t, int x) {
  tree *q;
  if (x < t->info)
    if (t->L == NULL) {
      q = new(tree); q->info = x;
      q->L = NULL;
      q->R = NULL;
      t->L = q;
    }
    else add(t->L, x);
  else if (t->R == NULL) {
    q = new(tree);
    q->info = x;
    q->L = NULL;
    q->R = NULL;
    t->R = q;
  }
  else add(t->R, x);
}
```

```c++
void main() {
  int x;
  tree* beg = new tree;
  cin >> x;
  beg->info = x;
  beg->L = NULL;
  beg->R = NULL;
  cin >> x;
  while (x != 0) {
    add (beg, x);
    cin >> x;
  }
}

// Удаление дерева двоичного поиска
void Destroy(tree *root) {
  if (root == NULL) return;
  Destroy(root->L);
  Destroy(root->R);
  delete root;
}
```

